# This is a server to control the stuffa application

### How to use

You need to install [dotnet-cli](https://docs.microsoft.com/en-us/dotnet/core/tools/?tabs=netcore2x) for your platform. After that:

    cd stuffa-remote
    dotnet run
